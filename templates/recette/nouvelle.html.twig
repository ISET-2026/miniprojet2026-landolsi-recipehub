{% extends 'base.html.twig' %}

{% block title %}Nouvelle recette — RecipeHub{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h1 class="fw-bold mb-4">➕ Créer une recette</h1>
            {{ form_start(formulaire) }}
                {% for field in ['titre', 'description', 'instructions', 'tempsPreparation', 'tempsCuisson', 'difficulte', 'nbPersonnes', 'categorie', 'tags', 'publiee', 'imageFile'] %}
                    <div class="mb-3">{{ form_row(formulaire[field]) }}</div>
                {% endfor %}
                {{ form_row(formulaire.enregistrer) }}
            {{ form_end(formulaire) }}
            <a href="{{ path('app_recettes') }}" class="btn btn-link mt-2 text-muted">← Annuler</a>
        </div>
    </div>
{% endblock %}